VITE_API_BASE_URL=/api
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
# Set to "true" to opt-in to httpOnly cookie mode for the refresh token.
# Backend must also have AFTERGLOW_REFRESH_COOKIE_ENABLED=true.
VITE_USE_REFRESH_COOKIE=false
